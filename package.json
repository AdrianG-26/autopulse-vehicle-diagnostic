{
  "name": "autopulse-vehicle-diagnostic",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd website && npm install && npm run build",
    "start": "cd website && npm install && npm install -g serve && serve -s build -l $PORT"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
